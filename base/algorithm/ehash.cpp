#include<bits/stdc++.h>
// #include<bits/extc++.h>
#define oo INT_MAX
#define ll long long
#define maxn  1000099
#define mp(a, b) make_pair(a, b)
#define _rep(i, a, b) for(int i = (a); i <= (b); ++i)
#define _for(i, a, b) for(int i = (a); i < (b) ;++i)
using namespace std;
// using namespace __gnu_pbds;

unsigned ll h[maxn], pre[maxn];

unsigned long long ehash(const char* str){
    
    return 0;
}